import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionwizard.ionreactbanking',
  appName: 'ionbanking',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
